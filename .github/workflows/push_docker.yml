name: Builds and pushes the docker image on the organization's hub

on:
  push:
    branches: ['main']
env:
  REGISTRY: docker.io
  ORG: ${{ github.repository_owner }}
  
jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: "swisscat@epfl.ch"
          password: "ARiS3h!u5gj#j"
      - name: build-and-push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: jcswisscat/em_rpi:latest@sha256:dd6622b55a6bb82ded3f74cb8580e4894e3a523c167e8f1407e682a4700cd41c
