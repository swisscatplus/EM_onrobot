profile_name: work_ubuntu_aruco_test
start_rviz: false
rviz_config: aruco_test_debug.rviz

movement:
  enabled: false

imu:
  enabled: false

localization:
  enabled: false

aruco_test:
  enabled: true
  camera_backend: opencv
  source: /dev/video4
  config: calibration_ubuntu_test.yaml
  marker_map_config: marker_map_laptop_test.yaml
  diagnostics_rate_hz: 1.0
  camera_frame: camera_frame
  map_camera_frame: camera_frame
  debug_image_topic: /aruco_test/debug_image
  marker_pose_topic: /aruco_test/marker_poses
  camera_pose_topic: /aruco_test/camera_pose
  summary_topic: /aruco_test/summary

marker_map:
  enabled: true
  config: marker_map_laptop_test.yaml
  diagnostics_rate_hz: 1.0

ekf:
  enabled: false

visualization:
  enabled: false
  backend: foxglove
  port: 8765
  address: 0.0.0.0

diagnostics:
  enabled: false
